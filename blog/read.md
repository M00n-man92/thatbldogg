###  `cd api`   to run the server

must before running the react frontend

###  `npm init`  to initialize

###  `npm start` starts the server on port 5000

an then on another terminal run the react app

###  `cd blog`

###  `npm init`

### `npm start`